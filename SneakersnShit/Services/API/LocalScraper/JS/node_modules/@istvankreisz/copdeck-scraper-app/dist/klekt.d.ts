import { StorePrices, StoreInfo, Scraper, Fetcher, Currency, APIConfig, ScraperConfig } from './types';
declare class KlektScraper implements Scraper {
    readonly store = "klekt";
    config: ScraperConfig;
    fetcher: Fetcher;
    set(fetcher: Fetcher): void;
    constructor();
    generateNewConfig(): void;
    setProxies(apiConfig: APIConfig): void;
    setUpConfig(apiConfig: APIConfig): void;
    getData: (apiConfig: APIConfig, request: () => Promise<any>) => Promise<any>;
    vendureToken(currency: Currency): string;
    search(searchTerm: string, apiConfig: APIConfig): Promise<Array<StoreInfo>>;
    getItemPrices(storeInfo: StoreInfo, apiConfig: APIConfig): Promise<StorePrices>;
}
declare const _default: KlektScraper;
export default _default;
