import { Item, SneakerAPI, Scraper, Fetcher, StoreId, APIConfig, ExchangeRates } from './types';
import { PriceCalculator } from './priceCalculator';
declare class SneakerFetcher implements SneakerAPI {
    scrapers: Scraper[];
    priceCalculator: PriceCalculator;
    fetcher: Fetcher;
    set(fetcher: Fetcher): void;
    getExchangeRates(apiConfig: APIConfig): Promise<ExchangeRates>;
    searchItems(searchTerm: string, apiConfig: APIConfig, store?: StoreId): Promise<Array<Item>>;
    getItemPrices(item: Item, apiConfig: APIConfig): Promise<Item>;
}
export { SneakerFetcher };
