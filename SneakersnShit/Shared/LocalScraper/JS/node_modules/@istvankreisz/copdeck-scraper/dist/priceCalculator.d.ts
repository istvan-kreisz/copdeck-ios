import { InventoryItem, SellerInfo, StoreId, ExchangeRates, CurrencyCode } from './types';
declare class PriceCalculator {
    stockxSellerPrice(price: number, currencyCode: CurrencyCode, sellerInfo: SellerInfo, exchangeRates?: ExchangeRates): number;
    stockxBuyerPrice(price: number, currencyCode: CurrencyCode, sellerInfo: SellerInfo, exchangeRates?: ExchangeRates): number;
    klektSellerPrice(price: number): number;
    klektBuyerPrice(price: number, currencyCode: CurrencyCode, sellerInfo: SellerInfo): number;
    goatSellerPrice(price: number, currencyCode: CurrencyCode, sellerInfo: SellerInfo, exchangeRates?: ExchangeRates): number;
    goatBuyerPrice(price: number, currencyCode: CurrencyCode, sellerInfo: SellerInfo, exchangeRates?: ExchangeRates): number;
    calculatePrice(storeId: StoreId, inventoryItem: InventoryItem, sellerInfo: SellerInfo, exchangeRates?: ExchangeRates): InventoryItem;
}
export { PriceCalculator };
